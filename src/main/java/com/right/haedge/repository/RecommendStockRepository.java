package com.right.haedge.repository;

import com.right.haedge.entity.RecommendStock;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RecommendStockRepository extends JpaRepository<RecommendStock, Long> {
}
